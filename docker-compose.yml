services:
  pricing-service:
    build:
      context: ./
      dockerfile: BE/LinguageFlowPOC/Dockerfile
    env_file:
      - .env
    environment:
      SERVER_PORT: ${SERVER_PORT:-8080}
    ports:
      - "${SERVER_PORT:-8080}:8080"
    restart: unless-stopped
    working_dir: /app
